extends ../../layouts/admin

block content
  section.section-padding
      hr.bottom-line
      .card.mb-3
        .card-block
          .card-header
            i.fa.fa-users 
            |  #{title}
            a(href='/admin/user/create' class='float-right btn btn-success') Add new
          .card-body
            .table-responsive
              table#dataTable.table.table-bordered(width='100%', cellspacing='0')
                thead
                  tr
                    th #
                    th Title
                    th(colspan=2) Actions
                tbody
                  - var i = 0

                  each user in users
                    - i++
                    tr
                      td= i
                      td 
                        a(href=user.url) #{user.email}
                      td
                        a(href=user.url+'/update' class="btn btn-warning") Edit
                      td
                        a(href=user.url+'/delete' class="btn btn-danger") Delete
